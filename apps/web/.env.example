# Point the web app at the NestJS API. Leave empty to run fully offline
# with the built-in local engine (recommended for the live demo fallback).
NEXT_PUBLIC_API_URL=
# Set only when deploying under a subpath, e.g. /wegweiser
NEXT_PUBLIC_BASE_PATH=

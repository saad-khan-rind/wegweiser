services:
  ollama:
    image: ollama/ollama
    restart: unless-stopped
    volumes:
      - ollama_data:/root/.ollama

  ai:
    build: ./apps/ai
    ports: ["4002:3002"]
    environment:
      - USE_EMBEDDINGS=0

  api:
    build: ./apps/api
    ports: ["4001:3001"]
    environment:
      - PORT=3001
      - WEB_ORIGIN=http://204.168.210.222:4000
      - AI_SERVICE_URL=http://ai:3002
      - OLLAMA_URL=${OLLAMA_URL:-http://ollama:11434}
      - OLLAMA_MODEL=${OLLAMA_MODEL:-llama3.1:8b}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
    depends_on:
      - ai
      - ollama

  web:
    build: 
      context: ./apps/web
      args:
        - NEXT_PUBLIC_API_URL=http://204.168.210.222:4001
    ports: ["4000:3000"]
    depends_on:
      - api

volumes:
  ollama_data:
    external: true